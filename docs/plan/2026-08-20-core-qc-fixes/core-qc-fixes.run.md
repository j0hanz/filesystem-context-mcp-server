# Run: Close the qc findings on `src/core/`

Executing [`core-qc-fixes.plan.md`](core-qc-fixes.plan.md), started 2026-08-20 at `5d882ed`.

- **1** 2026-08-20 — done. `recomputeAllowedDirectories` commits `rootBoundaries` + `initialize` together after all awaits; `setRoots` rolls back `rootDirectories` on rejection. New `path-guard-atomic.test.ts` (NUL-byte realpath → `ERR_INVALID_ARG_VALUE`). `node scripts/tasks.mjs` → `6/6 passed 31.4s`.
- **2** 2026-08-20 — done. `registrar.ts` finally wraps `setRoots` in try/catch (state → idle survives rejection); catch clears `this.rootDirectories = []`. New `roots-failure-recovery.test.ts`. Deviation from plan: TS still over-narrowed even via an annotated local, so applied the plan's fallback literally — widened `state` to `string` and deleted the `RootsManagerState` alias (comment explains why). `node scripts/tasks.mjs` → `6/6 passed 22.6s`.
- **3** 2026-08-20 — done. `NormalizedOptions`+`ReadMode`+`NormalizeResult` replaced by `NormalizedBase`+`NormalizedSpec` union; `normalizeSpec` returns the union; `FileReader` switches on `spec.kind` and reads fields directly; `requireOption` and the `Missing ${key}` error deleted; `ReadFileResult.readMode` → `ReadSpec['kind']`. No behavior change. `node scripts/tasks.mjs` → `6/6 passed 30.2s` (read-line-bounds, read-input-schema unchanged).
- **4** 2026-08-20 — done. Read pipeline moved to `src/core/read.ts` (850 lines): exports `ReadSpec`, `ReadFileResult`, `normalizeSpec`, `readNormalized` (ex `readFileWithStatsInternal`), `readFileWithStats`, `readFileBufferWithLimit`, `countLines`, `createTooLargeError`, `assertFileStats`, `STREAM_CHUNK_SIZE`. `fs.ts` trimmed to 369 lines, imports the moved symbols from `./read.js`; removed now-unused `ErrorCode`/`FsError`/`text`/`StringDecoder`/mime-helper/`isFsError` imports. Importers updated: `edit.ts`, `create.ts`, `read.ts`(tools), `replace-in-files.ts`, `read-line-bounds.test.ts` → `../core/read.js`. Trimmed a 4-line header, two decorative section dividers, and one verbose comment in `read.ts` to land exactly at the 850 target. `node scripts/tasks.mjs` → `6/6 passed 22.7s`; `wc -l` → fs.ts 369, read.ts 850, neither over 1000.
- **5** 2026-08-20 — done. Rewrote the `path.ts` waiver (was 8 lines claiming the file "splits only into <100-line fragments of a single concept"; now 20 lines naming the two concerns that actually live here — denylist policy and allowed-directory assembly — and the shared `IS_WINDOWS`/`normalizeForMatch`/`isPathWithinDirectories` primitives that keep them together). Verified every named symbol exists at its line. Comment-only. `node scripts/tasks.mjs --quick` → `4/4 passed 12.7s`.
- **6** 2026-08-20 — done. Deleted the six `GuardedFileSystem` pass-throughs + the defending comment (`resolvePathOrRoot`, `validateExistingPath`, `validateExistingDirectory`, `validatePathForWrite`, `validatePathForDelete`, `isAllowedRoot`). Inlined the four single-caller module helpers into their methods: `stat`→`.stat`, `mkdir`→`.mkdir`, `readlink`→`.readlink` (comment kept), `readFileRaw`→`.readRaw`. Left `setRoots`, `statDetailed`, `hasChildrenUnchecked` alone. Updated all 13 call sites to `fs.pathGuard.x(...)` / `ctx.fs.pathGuard.x(...)` across the 6 files the plan names: `list.ts`, `replace-in-files.ts`, `search-content.ts`, `search-files.ts`, `move.ts`, `delete-file.ts`. `node scripts/tasks.mjs` → `6/6 passed 30.1s`. Note on the verify: `git diff --stat -- src/tools` shows 9 files, not 6 — the extra 3 (`create.ts`, `edit.ts`, `read.ts`) are step-4 importer edits (2-line import changes), untouched by step 6. The plan's "exactly 6" assumes per-step commits (plan Notes: "Commit per step"); this run kept one uncommitted tree, so the diff is cumulative. Step 6's own contribution is exactly the 6 call-site files.
- **7** 2026-08-20 — done. `Problem.fromUnknown` now calls `classify` directly (UNKNOWN/IO_ERROR override preserved; caller `path` still wins over `problem.path`; details stay dropped as before). Deleted `DetailedError`, `createDetailedError`, `getSuggestion` (the `FsError` ctor and `resolveSuggestion` already read `DEFAULT_SUGGESTIONS` directly). Collapsed `isFsErrorCarrier` into `isFsError` (one exported structural check); `classify` and the `hasErrorShape` comment updated. In `errors.test.ts`: removed the `getSuggestion` and `createDetailedError` describes + their imports; the NOT_FOUND-suggestion behavior stays covered by the existing `fromUnknown` test (comment reworded off `getSuggestion`); renamed the `isFsErrorCarrier guard` describe to `isFsError guard`. `node scripts/tasks.mjs` → `6/6 passed 30.0s`; `grep -rn "createDetailedError\|getSuggestion\|isFsErrorCarrier" src __tests__` → no matches.
- **8** 2026-08-20 — done. Added `SKIPPABLE_FS_CODES` (ErrorCode set) and `SKIPPABLE_ERRNOS` (errno string set) to `errors.ts`, the dangling-symlink comment moved onto `SKIPPABLE_FS_CODES`. `glob.ts` catch now dispatches: `isFsError` → `SKIPPABLE_FS_CODES.has` else rethrow; then `isNodeError` + `SKIPPABLE_ERRNOS.has` else rethrow. One fix: `NodeJS.ErrnoException.code` is `string | undefined`, so the errno branch guards `error.code !== undefined` before `SKIPPABLE_ERRNOS.has`. `node scripts/tasks.mjs` → `6/6 passed 22.7s`.
- **9** 2026-08-20 — done. `KNOWN_BINARY_EXTENSIONS` now derived from `EXT_MAP` (non-`text` kinds) plus an explicit `EXTRA_BINARY_EXTENSIONS` remainder, with `svg` filtered out so the read path keeps probing SVG as text. Resolves the table drift toward binary (`.xz .bz2 .msi .dmg .tiff .aac .ogg .m4a` now binary; `.mp4 .docx .ttf .woff2` covered). Added two `isKnownBinaryExtension` cases to `mime.test.ts` (`a.svg` → false, `a.xz` → true). `node scripts/tasks.mjs` → `6/6 passed 22.7s`; `file-type.test.ts` untouched.
- **10** 2026-08-20 — done. Extracted `resolveConfiguredDirs(envVar, { allowMissing?, resolveReal? })` (module function after `resolveAllowedDirectoriesState`); both warning messages templated on `envVar` (verbatim). The FS_ALLOWED_DIRS loop → `resolveConfiguredDirs('FS_ALLOWED_DIRS', { allowMissing })`; the ROOT_BOUNDARY loop → `resolveConfiguredDirs('ROOT_BOUNDARY', { resolveReal: true })`. `allowMissing` read once before the first call. Lands on top of step 1's deferred-commit restructure (local `boundaries` kept). `node scripts/tasks.mjs` → `6/6 passed 24.5s` (`cli-dir-unify.test.ts`, `cwd-walk.test.ts` unchanged).
- **11** 2026-08-20 — done. Deleted `assignDefined` from `util.ts` and its describe in `util.test.ts` (+ import). Rewrote the two `search-files.ts` consumers with the spread idiom: `applySummaryFields` via `Object.assign` with conditional spreads (kept the `skippedInaccessible || undefined` falsy-zero drop as `summary.skippedInaccessible ? {...} : {}` — not "fixed"); the `searchOptions` literal inlines `maxDepth`/`signal` conditionally. Removed the `assignDefined` import. `node scripts/tasks.mjs` → `6/6 passed 22.6s` (`search-abort.test.ts` and search-files output assertions pass).
- **12** 2026-08-20 — done.
- **13** 2026-08-20 — done. Deleted `ProcessContext`; `processGlobPattern(pattern, plan, seen, onlyFiles, excludeFunc)` reads `cwd`/`maxDepth`/`suppressErrors` from `plan`. Inlined `nativeGlobEntries` + `globEntries` into one generator whose top does the `respectGitignore` load. Deleted `buildGlobOptions` + `GlobConfig`. `excludePatterns`/`includeHidden`/`baseNameMatch`/`onlyFiles` now optional on `GlobEntriesOptions` (exported it); `normalizeGlobOptions` defaults `baseNameMatch ?? false`, `excludePatterns ?? []`, `includeHidden` falsy; `onlyFiles ?? true` read at the use site (matching `buildGlobOptions`'s `true`). `engine.ts` drops the `buildGlobOptions` import and passes the literal to `globEntries` at both call sites. Fixed the dead-arg call in `createExcludeFilter` — was passing `''` for `absolutePath` via `isIgnoredByGitignore`, now calls `gitignoreMatcher.isIgnored(posixRel, isDir)` directly; `isIgnoredByGitignore` kept (`calculate-hash.ts` uses its `absolutePath` form). `node scripts/tasks.mjs` → `6/6 passed 30.1s`; `wc -l src/core/glob.ts` → 533.
- **14** 2026-08-20 — done. `rawPut` is now generic (`<E extends StoredEntry>`, `createFn: (base) => E`) → the two `entry as ...` casts gone. `getExisting` and `tryReturnHashHit` got concrete-kind overloads (no call-site casts) — `getExisting(uri,'text'|'blob')` returns the narrowed member; `getExisting(uri)` (no kind, for `getEntry`) added as a third overload. Hash once: `putText`/`putBlob` compute `computeSha256` + `estimateBytes` once and pass them down; `checkBeforePut(entryBytes)` and `tryReturnHashHit(kind, mimeType, name, contentHash)` take the precomputed values instead of re-deriving (the `data` param dropped from both). Six `as` casts → 0. `node scripts/tasks.mjs` → `6/6 passed 23.3s` (`resource-store.test.ts`, `resource-store-blob.test.ts` pass); `grep -c " as " src/core/store.ts` → 0 (≤1).
- **15** 2026-08-20 — done. Extracted `guardedEntries(entries, pathGuard, signal, counters)` — the shared skeleton (abort→`counters.stoppedByAbort`+return, `validateExistingPath` in try/catch→`counters.skippedInaccessible++`+continue, yield). Both `scanContent` and `searchFiles` now loop over `guardedEntries(...)` with a `counters` object; the caller-side `maxResults` break and (scanContent-only) `skippedTooLarge`/stat logic stay. Dropped scanContent's `if (entry.dirent.isDirectory()) continue` — `globEntries` defaults `onlyFiles:true` now, so directories aren't yielded (searchFiles never had the check). Gave both the same truncation contract: `SearchContentOutcome.summary` gained `stoppedReason?: 'timeout'|'maxResults'`, computed cap-wins-over-abort exactly as searchFiles; `search-content.ts` surfaces it via a new `stoppedReason` output-schema field + `buildStructuredSummaryFields` spread (matching `search-files.ts`). Moved `src/core/search/engine.ts` → `src/core/search.ts` (dir held one file, removed); fixed its 4 relative imports `../`→`./`; updated the 6 importers (`edit.ts`, `replace-in-files.ts`, `search-content.ts`, `search-files.ts`, `replace-dollar-expansion.test.ts`, `search-abort.test.ts`). Snapshot: the snapshot test re-serializes both sides with `JSON.stringify(…,null,2)`, so `FS_UPDATE_SCHEMA_SNAPSHOT=1` re-expanded the hand-compacted baseline into a 329-line cosmetic diff — reverted it and inserted only the 5-line `stoppedReason` block into the compact `search_text` outputSchema. `node scripts/tasks.mjs` → `6/6 passed 30.1s` (`search-abort.test.ts` unchanged); `ls src/core/search` → no such directory.
- **16** 2026-08-20 — done. `completePath(value, argumentName, contextArguments?)` — options bag gone; reads `this.pathGuard` directly (`getAllowedDirectories()`, `isSensitive.bind(this.pathGuard)`). `suggest` calls `this.completePath(value, argumentName, contextArguments)` instead of packing the bag. `argumentName` defaults to `''` in `suggest` (line 28), so the `?? ''` fallback in the old body went with the bag. `node scripts/tasks.mjs` → `6/6 passed 24.3s` (`path-completer.test.ts`, `completions.test.ts` pass).

## Done

- [x] `node scripts/tasks.mjs` → `6/6 passed 22.9s`.
- [x] New tests exist and pass: `path-guard-atomic.test.ts` (step 1, NUL-byte realpath → `ERR_INVALID_ARG_VALUE`), `roots-failure-recovery.test.ts` (step 2), the `processInParallel — timedSignal deadline` case in `abort.test.ts` (step 12), and the two `isKnownBinaryExtension` cases in `mime.test.ts` (`a.svg`→false, `a.xz`→true, step 9).
- [x] `wc -l src/core/*.ts` — only `path.ts` (1262) over 1000; it carries the step-5 waiver naming the denylist-policy and allowed-directory-assembly concerns. Largest others: `read.ts` 850, `glob.ts` 533, `errors.ts` 397, `path-completer.ts` 371, `store.ts` 349, `search.ts` 344, `mime.ts` 328.
- [x] `grep -rn "createDetailedError\|getSuggestion\|isFsErrorCarrier\|assignDefined\|buildGlobOptions\|requireOption" src __tests__` → no matches.
- [x] `ls src/core/search` → no such directory (moved to `src/core/search.ts`).
- [x] `git status` — every changed file is in `src/` or `__tests__/`; no touch of `transport.ts`, `server.ts`, `resources.ts`, `prompts.ts`, `package.json`, or `server.json`. Untracked `docs/` is the plan/run directory; `src/core/read.ts` is step 4's split.
