# Run: fix every Confirmed finding from the 2026-08-20 dev-branch bug hunt

Executing [`bug-hunt-fixes.plan.md`](bug-hunt-fixes.plan.md), started 2026-08-20 at `8f18abb`.

Drift check: `git diff --stat 8f18abb..HEAD -- src __tests__` → empty (zero drift, HEAD == `8f18abb`). Every Current state excerpt matched during plan-hunt.

- **1** 2026-08-20 — done. `node --test --import tsx "__tests__/tools/edit-multi.test.ts"` → `ℹ pass 18`, `ℹ fail 0`. `node scripts/tasks.mjs --quick` → `✓ 4/4 passed (2 skipped)`.
- **2** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/event-store.test.ts"` → `ℹ pass 7`, `ℹ fail 0`.
- **3** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/resource-subscribe-paths.test.ts"` → `ℹ pass 5`, `ℹ fail 0`. `node scripts/tasks.mjs --quick` → `✓ 4/4 passed (2 skipped)`. (registry: single-callback dedupe; subscribe: addCallback at live-subscription exits only.)
- **4** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/roots-failure-recovery.test.ts"` → `ℹ pass 2`, `ℹ fail 0`.
- **5** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/hunt-regressions.test.ts"` → `ℹ pass 21`, `ℹ fail 0` (1 skipped: platform symlink). `node scripts/tasks.mjs --quick` → `✓ 4/4 passed (2 skipped)`. **Deviation:** the plan prescribed stubbing `node:fs/promises` `realpath` via `mock.method`, but built-in ESM namespace exports are non-configurable and the module namespace is read-only in strict mode, so `realpath` cannot be patched from a consumer (`mock.method` → `Cannot redefine property`; direct assign → `Cannot assign to read only property`). Tested the same fix at its real consumer seam `isEntryAccessibleByType` (the `glob.ts:56-58` path the plan cites) with a fake `PathGuard` injecting `FsError(PERMISSION_DENIED)`, plus a second case asserting `UNKNOWN` is still rethrown (the plan's "do not over-widen" guard). Still platform-independent, still the behavior the fix changes. One type fix: `ERRNO_MAP[error.code]` indexed via `error.code !== undefined` guard (`code` is `string | undefined`).
- **6** 2026-08-20 — done. `node --test --import tsx "__tests__/tools/replace-text.test.ts"` → `ℹ pass 5`, `ℹ fail 0`. (Added `respectGitignore: !args.includeIgnored`; `resolveSearchRoot` returns `singleFile` for a file `path`, bypassing `globEntries`/`baseNameMatch`; removed now-dead `globEscape` + `basename` import.)
- **7** 2026-08-20 — done. `node --test --import tsx "__tests__/tools/directory.test.ts"` → `ℹ pass 30`, `ℹ fail 0`. (glob `getRelativeDepth` → `return count`; list.ts compensation `maxDepth: options.maxDepth - 1`. Existing `list` maxDepth=1/2 tests pass unchanged; new `find_files maxDepth:0` confirms base-only.)
- **8** 2026-08-20 — **STOP**. Source change applied: `suppressErrors: true,` added to both `globEntries` calls (`src/core/search.ts:184` scanContent, `:302` searchFiles). **Test infeasible — needs user decision.** The step demands a test that "forces a mid-walk glob error (mock/stub the glob iterator or `node:fs/promises` `readdir`/`glob` to throw after yielding one entry), asserting the call resolves with the matches collected so far instead of rejecting." Three blockers, all verified against the repo's pinned runtime (`engines.node` `>=24`):
  1. **The prescribed mock is ESM-forbidden.** `fsGlob` is `import { glob as fsGlob } from 'node:fs/promises'` (`glob.ts:1`). Built-in ESM namespace exports are non-configurable (`writable:true, configurable:false`), so `mock.method` → `Cannot redefine property: realpath`/`glob` and strict-mode direct assign → `Cannot assign to read only property` — same wall that forced the Step 5 deviation.
  2. **No real-disk mid-walk throw exists.** Node's `fs.glob` swallows directory-read failures: `PromisePrototypeThen(readdir(...), null, () => [])` (`internal/fs/glob.js`, identical in v22 and v24). A `chmod 000` subdirectory → readdir rejects EACCES → swallowed → empty array, not an iterator throw. Probed locally: a symlink loop also does not throw (cycle-detected, terminates, no error). An `exclude` callback that throws is also swallowed (probed: `BOOM_EXCLUDE` caught, iteration continued, no throw).
  3. **The gate the fix routes through is dead code on the pinned runtime.** `processGlobPattern`'s two catches (`glob.ts:458-460` start-of-scan, `:467-472` mid-walk) can only fire if `fsGlob`'s async iterator throws. Its only throw source is an AbortSignal, and `globEntries` passes no signal. So under Node ≥24 the `suppressErrors` flag has no observable effect — the fix is behavior-neutral consistency with the two existing exemplars.
  - **Exemplar precedent:** the two cited `suppressErrors: true` exemplars (`src/tools/replace-in-files.ts:599`, `src/tools/calculate-hash.ts:156`) have **zero** test coverage of this gate — `grep suppressErrors __tests__` → no matches. The repo already treats this flag as defensive/unt-testable.
  - **Implication:** the underlying bug-hunt finding's premise ("mid-walk errors reach `processGlobPattern` and rethrow when `suppressErrors` is false") is false for this runtime. A loader-hook mock could force a throw, but it would test a path that cannot fire in production on Node ≥24.
  - **Verify state:** `node --test --import tsx "__tests__/unit/search-abort.test.ts"` → `ℹ fail 0` passes on existing tests with no new test added.
  - **Decision for user:** (a) accept the source change as defensive consistency (matches exemplars, behavior-neutral, no test) and proceed; (b) revert the source change (fix targets a condition that can't occur on the pinned runtime); (c) invest in a custom ESM-loader mock to force an iterator throw despite it being production-unreachable. Stopping here; Steps 9-13 not started.
- **8** 2026-08-20 — done (deviation, per user decision (a)). `node --test --import tsx "__tests__/unit/search-abort.test.ts"` → `ℹ fail 0` (existing tests, no new test added). Source change kept as defensive consistency with the `replace-in-files.ts`/`calculate-hash.ts` exemplars; behavior-neutral on Node ≥24 where `fs.glob` swallows mid-walk errors. No test written: the prescribed mid-walk-error trigger is ESM-unmockable and has no real-disk equivalent on the pinned runtime, so any test would either be vacuous (passes with or without the flag) or exercise a production-unreachable loader-mocked path.
- **9** 2026-08-20 — done. `node --test --import tsx "__tests__/tools/elicitation.test.ts"` → `ℹ pass 14`, `ℹ fail 0`. (`delete-file.ts:222-233` extended the type guard with `identityChanged` = `dev`/`ino`/`birthtimeMs` comparison against the pre-elicitation `itemStats.stats`; failure branch unchanged. New test swaps the dir during the elicitation gap — `rm`+`mkdir` same path with a `marker.txt` — and asserts structured `ok:false`, failure code `INVALID_INPUT`, and the marker survives. Probe confirmed `ino`+`birthtimeMs` differ on same-path dir recreate on this machine; `dev` stays equal, as the plan noted for non-POSIX drivers. Fix: first draft used `assertToolError`, but the delete tool returns failures as structured `ok:false` (per the existing decline test), not `isError:true` — switched to the structured `ok:false` + `failures[0].error.code` + marker-survives assertions.)
- **10** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/path-completer.test.ts"` → `ℹ pass 8`, `ℹ fail 0`. (`findMatchesInDirectory` rewritten to stream via `opendir` + break at `MAX_COMPLETION_ITEMS` (`dir.close()` in `finally`); `node:fs/promises` import `readdir`→`opendir`. `findMatchingRoots` uses `isSamePath(rootDir, normalizedSearchDir)` (imported from `./path.js`) instead of a `normalizePath` string compare. Two new tests: (1) 150 files → `results.length === 100` (cap); (2) two sibling allowed roots under a non-allowed parent, `suggest(lowercased-parent + sep)` surfaces both roots via `findMatchingRoots` case-fold — gated `{ skip: platform !== 'win32' }` since the fold is Windows-only at this step; on case-sensitive Linux the strict compare is already correct, so the fix is a no-op there and skipping loses no fix coverage.)
- **11** 2026-08-20 — done. `node --test --import tsx "__tests__/unit/path-guard.test.ts" "__tests__/tools/move.test.ts"` → `ℹ pass 29`, `ℹ fail 0`; `node scripts/tasks.mjs --quick` → `✓ 4/4 passed (2 skipped)`. (`normalizeCaseForComparison` folds on `IS_WINDOWS || platform()==='darwin'` via `IS_CASE_INSENSITIVE_FS`; `isSafeGlobSyntax` doc comment now names `isEntryAccessibleByType`/`isPathWithinDirectories`/`validateExistingPathDetailed`. New move test: case-only rename `Foo.txt`→`foo.txt` asserts a single rename with no overwrite-confirmation elicit (never-fire handler). **Deviation/findings:** (a) the plan said gate "the same way the existing test gates on win32/darwin" but also "only runs on a real darwin host" — gated **darwin-only**, because empirically on win32 the move tool **no-ops** a case-only rename: `move.ts:248` (`resolvedSource === resolvedDest`) resolves the destination to the source's actual on-disk casing (via `validatePathForWrite`), so `continue` fires before any rename (`moves: []`, no failure). That no-op is pre-existing `move.ts` behavior, out of Step 11's scope (no `move.ts` change); the win32 leg is covered by the existing case-variant subdir test. CI has no macOS runner, so the darwin case self-verifies on a real Mac only, per the plan. (b) `--quick` surfaced 5 pre-existing-since-Steps-6/7 lint/type errors that the per-step single-file Verifies had not caught: 3× `no-unsafe-return` in `directory.test.ts` (my Step 7 test used `getStructured<{...}>` on a non-generic helper returning `Record<string,unknown>` — switched to the repo's `const sc = getStructured(raw); const results = sc['results'] as {path:string}[]` pattern), 1× `no-empty-function` in `path-completer.ts` (empty `.catch(() => {})` → added a comment body), 1× `require-await` in `replace-in-files.ts` (the Step 6 `singleFileEntry` async generator has no await — kept the async generator to satisfy the `AsyncIterable` contract of `processEntriesConcurrently` and added a targeted `eslint-disable-next-line @typescript-eslint/require-await`, matching the repo's existing disable pattern in `move.ts`).)
- **12** 2026-08-20 — done (deviation, per Step 8 precedent). `node --test --import tsx "__tests__/tools/search-text-pagination.test.ts" "__tests__/tools/find-files-pagination.test.ts"` → `ℹ pass 7`, `ℹ fail 0`. Source: `const fetchMax = Math.min(cursorOffset + pageSize, MAX_SEARCH_RESULTS);` at `search-content.ts:359` and `search-files.ts:138` (was `cursorOffset + pageSize`). **search_text guard added** (deterministic): extends `search-text-pagination.test.ts` with a `big.txt` of `MAX_SEARCH_RESULTS + 1` matching lines + `cursor: encodeOffsetCursor(MAX_SEARCH_RESULTS * 5)` + `maxResults: 5`; asserts `totalMatches <= MAX_SEARCH_RESULTS`. One file ⇒ scan is sub-200ms (no timeout interference). **Verified the guard bites**: reverting `search-content.ts` to `cursorOffset + pageSize` flips the test to `✖ fail 1` (`totalMatches` 10001 > 10000); restored. **find_files mirror — INFEASIBLE, dropped.** To observe the clamp on `find_files` requires collecting >`MAX_SEARCH_RESULTS` (10000) files, but `guardedEntries` (`search.ts:152-164`) runs `pathGuard.validateExistingPath` (stat + realpath) per entry at ~1900 files/s on this machine, so a >10000-file scan exceeds the tool's `DEFAULT_SEARCH_TIMEOUT_MS` (5000). Measured with the fix: `totalMatches=9497, stoppedReason=timeout` (clamps never reached 10000); with the bug: `totalMatches≈9856, stoppedReason=timeout`. Both ≤10000 ⇒ the assertion passes with **and** without the clamp — a vacuous guard (ponytail forbids). The find_files clamp is unreachable within the 5s timeout on the pinned runtime; the source change is kept as defensive consistency mirroring `search-content.ts` (same `Math.min` expression; would bite on a faster runtime or a raised timeout). The plan's Verify (`ℹ fail 0`) is met; the search_text guard exercises the identical clamp logic.
- **13** 2026-08-20 — done. `node --test --import tsx "__tests__/prompts-stdio.test.ts"` → `ℹ pass 2`, `ℹ fail 0`; `node scripts/tasks.mjs` → `✓ 6/6 passed`. Source: dropped `import { pathToFileURL } from 'node:url'`; added `import { buildFileResourceUri } from './core/file-uri.js'`; `linkToPath` now `uri: buildFileResourceUri(absPath)` (was `pathToFileURL(absPath).href`). Test: added `assert.strictEqual(m1.content.uri, buildFileResourceUri(normalizePath(filePath)))` after the existing `m1.content.type === 'resource_link'` assertion. **Deviation:** the plan specified `buildFileResourceUri(filePath)` (raw), but the server normalizes the path through `validateExistingPath` → `normalizePath` (`path.ts:186-195`), which lowercases the Windows drive letter; the raw `filePath` keeps the uppercase drive from `os.tmpdir()`, so exact equality failed on win32 (`c%3A` vs `C%3A`). Applied `normalizePath` to the expected path (imported from `../src/core/path.js`) to mirror the server's normalization — preserves the plan's exact-equality intent and guards both the scheme and the encoding. **Schema snapshot regenerated:** Step 1's `edit.ts` `.max(MAX_EDITS_PER_FILE)` + `(max 100)` describe text changed the emitted `edit` tool inputSchema; `__tests__/schemas/snapshot.test.ts` (a regression baseline not named in the plan) drifted on the `edit` tool only (confirmed: `grep`-isolated the drift to `edit`, no other tool). Regenerated via `FS_UPDATE_SCHEMA_SNAPSHOT=1 node --test --import tsx "__tests__/schemas/snapshot.test.ts"` — a necessary fixture update consequent on Step 1, not scope creep. (`__tests__/tools/find-files-pagination.test.ts` is unchanged in `git status` — the Step 12 cap test was added then reverted to net-zero.)

## Done

- [x] `node scripts/tasks.mjs` → `✓ 6/6 passed 31.1s` (format, lint, type-check, knip, rebuild, test — no skips, no failures).
- [x] Every new `it(...)` named in Steps 1-13 is present and passing (full-gate `test` task is green; per-step Verifies all `ℹ fail 0`). Step 12's find_files mirror was dropped as infeasible (see Step 12 log); search_text guard exercises the identical clamp.
- [x] `git status` shows changes only in the 15 in-scope `src/` files plus their named test files. (Extra: `__tests__/schemas/__snapshots__/tool-schemas.json` — a fixture regenerated for Step 1's schema change, not a named test file; `docs/` untracked, holds this plan + run log.)
- [x] `grep -rn assertSafeGlob src` → zero matches.
- [x] `grep -rn "pathToFileURL" src/prompts.ts` → zero matches.
