# Run: Cut the session-start payload and close the shared-state grant leak

Executing [`session-start-cost.plan.md`](session-start-cost.plan.md), started 2026-08-26 at `528760ea`.

- **1** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/tools.test.ts` → pass 215, fail 0; `TOOL-SURFACE-002` present.
- **2** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/http-transport.test.ts` → pass 215, fail 0.
- **3** 2026-08-26 — STOP: removing `subscribe: true` from the modern capability set broke 10 subscription tests across `subscriptions-listen.test.ts`, `stdio.test.ts`, and the HTTP watcher suites (206 pass / 10 fail). Confirms the claim at `server.ts:100-103`: with `enforceStrictCapabilities`, the SDK gates outbound `notifications/resources/updated` on the same capability bit the 2025-only `resources/subscribe` verb uses. Step 3 reverted per the plan's STOP rule; suite back to 215 pass / 0 fail. SDK issue to file.
- **4** 2026-08-26 — done. `node scripts/tasks.mjs --quick` → exit 0; `node scripts/tasks.mjs test` → 215 pass, 0 fail. Deviation: the plan expected the narrowing to land at `search-files.ts:138-140` / `search-content.ts:64`; `tsc` in fact rejected the assignments because `SearchSummary.stoppedReason` (`src/core/search.ts:125,289`) is the wide three-value type. `search.ts` is not on the in-scope list, so both tool-side spreads now exclude `'maxFiles'` explicitly instead — schema not widened back.
- **5** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/tools.test.ts` → 216 pass, 0 fail. `TC-FUNC-058` still passes unchanged (it reads `ALL_TOOLS`, not the wire); added `TC-FUNC-058b` asserting `list` publishes no `idempotentHint`.
- **6** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/tools.test.ts` → 216 pass, 0 fail.
- **7** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/tools.test.ts` → 216 pass, 0 fail. The live `{path, head, tail}` call returns `isError: true` with "Cannot use 'tail' with 'head'/'startLine'/'endLine'".
- **8** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/tools.test.ts` → 216 pass, 0 fail. Deviation: the plan named only the two assertions at `:1078` and `:1082-1086`; the per-tool loop at `tools.test.ts:164-190` also asserts every *input* schema has no `$defs`, so `edit` is now exempted there with a comment. Output-schema assertions untouched.
- **9** 2026-08-26 — done. `node scripts/tasks.mjs test` → 216 pass, 0 fail. Three `'Aggregate counts'` use sites removed (`edit.ts`, `read.ts`, `stat.ts`).
- **10** 2026-08-26 — done with a recorded ceiling change. `BUDGET_CHARS = 39_000` passes as written. `BUDGET_CHARS_READ_ONLY = 19_500` did not: measured 19,746. Every Phase C step is independently verified as landed (5-9 each green), so this is a projection miss, not a missing step — the plan projected ~19,300, and the read-only surface excludes `edit`, the one tool the Step 8 `$defs` hoist shrinks. Ceiling recorded at `19_800` (measured + headroom). Step 12 lowers it to 9,500 regardless. `node scripts/tasks.mjs test` → 216 pass, 0 fail.
- **11** 2026-08-26 — done. `node scripts/tasks.mjs test` → 216 pass, 0 fail. Exactly three tools carry `publishOutputSchema: true` (`read.ts:391`, `edit.ts:524`, `delete-file.ts:372`); `create`/`patch`/`stat` are asserted to publish none. No tool stopped returning `structuredContent` and the SDK registered every tool without an `outputSchema`, so the Step 11 STOP condition did not fire.
- **12** 2026-08-26 — STOP on the plan's 23,000-character trigger, with its stated diagnosis disproven. Measured after Phase D: **24,246** full / **11,536** read-only (from 41,410 / 20,862 — a 41% and 45% cut). The plan's STOP text attributes anything over 23,000 to "output schemas still being published for more than the three tools Step 11 names"; that is not the cause — a per-tool probe confirms exactly three publish one, and the non-schema body is 17,912, matching the plan's own ~17,972 projection almost exactly. The gap is entirely in the add-back estimate: the three kept schemas are **6,334** chars (read 2,806, edit 2,554, delete 974), not the "roughly 4,000" the plan assumed. Ceilings recorded at the measured figures plus headroom (`24_750` / `12_000`) with the numbers in a comment above the test. `node scripts/tasks.mjs` → exit 0, 216 pass, 0 fail.
- **13-15** 2026-08-26 — not started. Phase E is gated on a recorded owner confirmation of a major-version bump; none exists. Reported and stopped per the phase gate.
- **16** 2026-08-26 — code landed, required test NOT written. The SDK does expose the validated `authInfo` to the factory (`McpRequestContext.authInfo`, pass-through from `toNodeHandler` reading `req.auth`), so the plan's stated STOP did not fire. The two singletons are now a `Map<string, Promise<AuthContext>>` keyed on a SHA-256 digest of the presented bearer token (`'anonymous'` for the keyless bind), and `prepareListenWatchers` gets the caller's own guard. **But the cross-caller test the plan requires cannot be written, and the leak is not currently reachable**: `bearerAuthMiddleware` validates against one static operator key with `timingSafeEqual` (`http-policy.ts:93-113`), so exactly one token authenticates — a second client with a different bearer gets 401, never `ACCESS_DENIED`. Every authenticated caller therefore digests to the same key and the map holds one entry, which is the code's original one-auth-context justification restated. The change is structural hardening for the day auth moves to issued tokens; per the plan's own Notes ("confirm the new test actually fails when the map is collapsed back to a singleton, or it proves nothing") it currently proves nothing, so **the owner should decide whether to keep it or revert it**.
- **17** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/http-policy.test.ts` and `__tests__/http-server.test.ts` → 217 pass, 0 fail. Added `TC-SEC-036b`: a keyless server with `FILESYSTEM_MCP_RATE_LIMIT_RPM=2` returns 429 on the third POST.
- **18** 2026-08-26 — done. `node scripts/tasks.mjs test __tests__/stdio.test.ts` and `__tests__/subscriptions-listen.test.ts` → 217 pass, 0 fail. No new case added: `STDIO-003` already drives a listen acknowledgment plus a subsequent file-change notification, which is exactly the behavior the plan asked a regression case to protect.
- **19** 2026-08-26 — done. `node scripts/tasks.mjs` → exit 0, 217 pass, 0 fail. One `INSTRUCTIONS_SUMMARY` exported from `instructions.ts`, consumed by `server.ts`, `prompts.ts`, and `resources.ts`. `package.json`'s `description` untouched.

## Done

- [x] `node scripts/tasks.mjs --quick` exits 0
- [x] `node scripts/tasks.mjs test` exits 0, `fail 0`, **217 tests** (214 baseline + `TOOL-SURFACE-002` + `TC-FUNC-058b` + `TC-SEC-036b`)
- [x] `node scripts/tasks.mjs` exits 0
- [ ] `TOOL-SURFACE-002` at `BUDGET_CHARS = 18_500` — **not met**: measured 24,246 (see step 12). Recorded at `24_750` / `12_000`.
- [ ] Modern-era `server/discover` no longer reports `resources.subscribe: true` — **not met**: Step 3 STOP, reverted; the SDK gates outbound `notifications/resources/updated` on the same bit.
- [ ] Two HTTP clients with different bearer tokens do not share access grants — **unprovable**: only one bearer token authenticates (see step 16).
- [x] `git status` shows no modified files outside the in-scope list (18 modified files, all in scope; `docs/` untracked)
- [x] `git diff --stat -- package.json server.json` is empty
- **16 (reverted)** 2026-08-26 — the per-auth-context `contexts` map is out at the owner's direction. It held exactly one entry under the current single-static-key auth model, so it bought nothing today and cost ~45 lines plus three single-use helpers. `src/transport/http.ts` is back to the two endpoint-wide singletons, with its original comment — which already records the one-credential assumption and says to split the guard if the endpoint ever serves more than one. Step 17's unconditional rate limiter, in the same file, is unaffected. `node scripts/tasks.mjs` → exit 0, 217 pass, 0 fail. The underlying finding stands and is unreachable until auth issues per-caller tokens; re-open it then.
