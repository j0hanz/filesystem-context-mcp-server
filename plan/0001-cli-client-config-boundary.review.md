VERDICT: READY
FILES_TOUCHED: none
SUMMARY: Both previously flagged issues are now resolved and verified against the current files. TASK-003's description of the dynamic `await import('./cli.js')` destructure as a top-level statement (src/index.ts:24-32) consumed by `main()` (starting at src/index.ts:120) matches the real file exactly. REQ-006 plus the updated TASK-001/TASK-002 correctly and completely resolve all five cross-boundary dependencies found in the prior review: `CliExitError` imported as-is (already exported), `IS_WINDOWS` and `validateCliPath` newly exported in cli.ts because both are used by bootstrap code outside the moved range as well as by the moved range itself, and `CONFIG_KEY_MAP`/`resolveConfigKey`/`ENV_DIR_SEP` moved wholesale into client-config.ts because all of their usages are confined to lines 525-1075. A full re-read of the moved range (525-1075) found no other unexported/unaccounted-for cli.ts bootstrap symbol referenced by the installer code. The architecture-brief.json edits (adding src/cli.js to the dependency list, removing the stale normalizePath entry) are also confirmed accurate against current source.
EVIDENCE:

- TASK-003 re-check: src/index.ts:24-32 is a top-level `const { CliExitError, parseArgs, runPrintConfig, allowPath, disallowPath, listAllowedPaths, manageConfig } = await import('./cli.js');` statement, executed at module load before `main()` is defined. `main()` (src/index.ts:120-213) contains zero `await import(...)` calls itself — it consumes the already-bound `allowPath`/`disallowPath`/`listAllowedPaths`/`manageConfig` directly (e.g. src/index.ts:173 `await allowPath(...)`). This now matches the plan's revised wording ("a top-level destructure... consumed by main()") exactly — no mismatch remains.
- Re-verified all 17 symbol line anchors unchanged and still exact (src/cli.ts is still 1075 lines): ClientConfigTarget:525, getExistingConfigPaths:530, writeJsonAtomic:585, ModifyOptions:621, findServerEntry:629, acquireLock:667, readOrCreateConfig:712, tryComparePaths:725, readDirsFromEnv:737, updateEnvForPath:746, modifySingleConfig:773, getTargetConfigs:832, allowPath:842, disallowPath:877, listAllowedPaths:916, modifyConfig:960, manageConfig:1048.
- Cross-boundary dependency check (the core of this re-verification): read all of src/cli.ts:1-524 (bootstrap) and all of src/cli.ts:525-1075 (moved range) in full.
  - `CliExitError` (defined :53, already `export class`) — used in moved range at :721, :863, :989, :1011, :1016, :1033, :1066. No export change needed; "imported as-is" is correct.
  - `validateCliPath` (defined :63, currently `function`, NOT YET exported — TASK-002 still pending) — used in bootstrap's `parseArgs` at :377 and :392, AND in moved range at :843 (`allowPath`) and :881 (`disallowPath`). Confirmed used by BOTH sides — exporting it (rather than moving it) is the correct call, since moving it would break bootstrap's `parseArgs`.
  - `IS_WINDOWS` (defined :29, currently `const`, NOT YET exported) — used in bootstrap's `deduplicateAllowedDirectories` at :289, AND in moved range's `listAllowedPaths` at :949. Confirmed used by BOTH sides — exporting is correct, moving would break bootstrap.
  - `CONFIG_KEY_MAP`/`resolveConfigKey`/`ENV_DIR_SEP` (defined :31, :37-47, :49-51) — grepped all occurrences in src/cli.ts: ENV_DIR_SEP only at :31 (def), :741, :759, :767 (all inside moved range); CONFIG_KEY_MAP/resolveConfigKey only at :37-51 (def) and :1054 (inside moved range, manageConfig). Zero occurrences in bootstrap (1-524). Confirmed moved-only — moving wholesale (not exporting) is correct.
  - Full read of moved range line-by-line found no other identifier defined in bootstrap (1-524) referenced anywhere in 525-1075: confirmed `getSystemErrorDetails`, `normalizeDirectoryError`, `assertDirectory`, `validateDirectoryPath`, `normalizeAndValidateDirs`, `deduplicateAllowedDirectories`, `parsePortOption`, `CLI_PARSER_CONFIG`, `parseArgs`, `printHelpAndExit`, `printVersionAndExit`, `normalizeCliExitMessage`, `OPTIONS_HELP`, `ENV_HELP`, `EXAMPLES_HELP`, `HelpRow` — none appear in the moved range. No sixth undocumented dependency exists.
- docs/adr/architecture-brief.json re-checked: dependency list (lines 15-25) now includes `"src/cli.js (CliExitError, IS_WINDOWS, validateCliPath — latter two newly exported)"`, matching the resolved REQ-006. The stale `normalizePath` entry is gone — only `"src/core/path.js (isSamePath)"` remains, matching actual usage (only `isSamePath` is called, at src/cli.ts:727; `normalizePath` is not used in 525-1075).
- TASK-004/test-file description re-confirmed still accurate: **tests**/unit/config-helper.test.ts:7-14 still imports `allowPath, disallowPath, getExistingConfigPaths, listAllowedPaths, parseArgs, writeJsonAtomic` from `'../../src/cli.js'` (pre-execution state, as expected — TASK-004 hasn't run yet).
- No new issues found. No remaining gaps in the plan/spec/architecture-brief against current source.
  ready_for_execution: true
  findings: []
